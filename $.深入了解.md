## 深入了解

#### 各种变量的存储位置以及创建和销毁

| **变量类型** |  **存储位置**  |      **创建时机**      |  **销毁时机**   | **关键字**       |
| :----------: | :------------: | :--------------------: | :-------------: | ---------------- |
|   局部变量   |       栈       |    进入函数/代码块     | 退出函数/代码块 | `auto`（默认）   |
| 静态局部变量 |  数据段/BSS段  | 程序启动（初始化一次） |    程序结束     | `static`         |
|   全局变量   |  数据段/BSS段  |        程序启动        |    程序结束     | 无（或`extern`） |
| 动态分配变量 |       堆       |   `malloc`/`calloc`    |     `free`      | 无               |
|  寄存器变量  | 寄存器（可能） |       同局部变量       |   同局部变量    | `register`       |
|   全局常量   |     代码段     |        程序启动        |    程序结束     | `const`          |

**1. 数据段（Data Segment）**

- 存储内容：

  - 已初始化的**全局变量**和**静态变量**（包括`static`局部变量）。

    例如：

    ```
    int global_init = 100;         // 全局变量（已初始化 → 数据段）
    static int static_init = 200;  // 静态变量（已初始化 → 数据段）
    ```

- 特点：

  - 变量在程序**编译时**被赋予明确的初始值（非零）。
  - 这些初始值会直接写入**可执行文件**，程序加载时由操作系统拷贝到内存的数据段。
  - 占用磁盘空间（因为存储了初始值）。

**2. BSS段（Block Started by Symbol）**

- 存储内容：

  - 未初始化的**全局变量**和**静态变量**（或显式初始化为`0`的变量）。

    例如：

    ```
    int global_uninit;            // 全局变量（未初始化 → BSS段）
    static int static_uninit;      // 静态变量（未初始化 → BSS段）
    int global_zero = 0;           // 显式初始化为0 → BSS段
    ```

- **特点**：

  - 变量在程序**启动时**由系统自动初始化为`0`（或`NULL`）。
  - **不占用磁盘空间**：可执行文件只需记录BSS段的大小，无需存储零值。



**查看程序的内存布局（Linux）**

通过`size`命令可查看编译后程序的各段大小（Text/Data/BSS）：

```sh
gcc program.c -o program
size program
```

输出示例：

```sh
text    data     bss     dec     hex filename
1000     200     100    1300     514 program
```

- **内存受限时**：需谨慎使用全局/静态变量，避免BSS段占用过多RAM。
- **启动时间**：BSS段清零操作可能影响嵌入式设备的启动速度（可通过启动代码优化）。

理解数据段和BSS段有助于优化程序内存占用，尤其在资源紧张的嵌入式环境中



#### 字符串赋值时常见误区

- 先定义char数组或指针，后直接赋值字符串

  ```c
  char arr[10],*p;
  arr="asa";//非法，不可直接赋值常量给数组
  p="asa";//合法，不建议直接赋值常量，后续不可更改其值
  char *p="aaa";//建议，此时存在于内存中（结合数据段/BSS段理解）
  char a[2]={"asda"};//非法，缓冲区溢出
  ```

- 浅拷贝导致指针悬挂

  ```c
  char *a,*b,c='c';
  a=&c;
  //浅拷贝
  b=a;//导致两个指针指向同一个地址（共享指针）
  //深拷贝
  b=strdup(a);//重新分配内存
  ```




#### typedef的使用

> 使复杂的数据类型和程序更加简洁易懂

- 枚举中使用

  ```c
  //原始方法,繁琐
  enum COLOR{
  		R,G,B
  	};
  	enum COLOR color;
  	color=R;
  //---------typedef--------------------
  typedef enum {
      R,G,B
  } Color;
  //使用
  Color red=R;
  ```

- 函数指针中使用

  ```c
  int (*fp)(int,int);
  fp=&fun;
  
  typedef (*Fp)(int,int);
  Fp fp=&func;
  ```

- <span id="struct">结构体中使用</span>

  ```c
  struct Person{
      int age;
      char *name;
  };
  struct Person p;
  //------typedef--------
  typedef struct {
      int age;
      char *name;
  } Person;
  Person p;
  ```

- 简化模板化编程（泛型）

##### 区别于#define

1. #define又预编译器处理，typedef由编译器处理
2. #define可以为数值和数据类型起别名，typedef只能为数据类型起别名