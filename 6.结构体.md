## 结构体

>  数组允许定义可存储相同类型数据项的变量，结构*允许您存储不同类型的数据项*

### 定义

1. 声明结构体名，同时定义结构体变量

   ```c
   struct Person{
       int age;
       char *name;
       char *iphone;
   } p1;
   ```

2. 直接定义结构体变量

   ```c
   struct {
       int age;
       char *name;
       char *iphone;
   } p1;
   ```

3. 声明结构体名，不定义变量

   ```c
   struct Person{
       int age;
       char *name;
       char *iphone;
   };
   ```

4. > [!NOTE]
   >
   > 使用typedef，可以简化复杂数据类型的定义；参考[深入了解章节中的typedef的使用](./$.深入了解#struct)

   

   

### 初始化

定义时进行初始化

```c
struct Person{
    int age;
    char *name;
} p={18,"zsf"};
```



### 访问结构成员

```c
struct Person{
    int age;
    char *name;
};


struct Person p1,p2;
p1.age=18;
strcpy(p1.name,"zsf");
p2.age=21;
strcpy(p2.name,"zsb");
```



### 结构体指针

> 指向结构体的指针，访问成员时需要使用`->`

```c
struct Person{
    int age;
    char *name;
} p1;

struct Person *sp=&p1;
sp->age=10;
```



### 结构体大小的计算

> [!NOTE]
>
> - 对齐的目的是提升CPU访问速度（未对齐可能导致性能下降或硬件异常）。
> - 结构体大小 ≠ 成员大小简单相加，必须考虑对齐和填充。
>
> 对齐系数：为基本数据类型的大小，对齐值 = 最大的数据类型对齐值；嵌套结构体 = 内部最大类型对齐值

#### 示例

```c
struct Example1 {
    char a;      // 1字节（偏移量0）
    int b;       // 4字节（偏移量需是4的倍数，故填充3字节）
    short c;     // 2字节（偏移量8）
};               // 总大小：12字节（需是4的倍数）
```

```sh
内存布局：
| a | 填充3 | b (4字节) | c | 填充2 |
0   1     4          8   10    12
```

#### 调整成员顺序优化空间

```c
struct Example2 {
    char a;      // 1字节（偏移量0）
    short c;     // 2字节（偏移量2）
    int b;       // 4字节（偏移量4）
};               // 总大小：8字节（无填充）
```

```SH
内存布局：
| a | c (2字节) | b (4字节) |
0   2          4          8
```

#### 修改对齐

> 强制指定对齐值，过度使用可能导致性能下降（CPU访问未对齐数据更慢）

```c
#pragma pack(1)     // 按1字节对齐（无填充）
struct Packed {
    char a;         // 1字节
    int b;          // 4字节（偏移量1）
};                  // 总大小：5字节
#pragma pack()      // 恢复默认对齐
```

#### 扩展

**offsetof** 宏：计算结构体中某个成员相对于结构体起始地址的**字节偏移量**。

> 只能用于普通结构体，不能用于位域

```c
#include <stddef.h>
#include <stdio.h>

struct Example {
    char a;     // 偏移量 0
    int b;      // 偏移量 4（因对齐填充3字节）
    short c;    // 偏移量 8
};

int main() {
    printf("Offset of b: %zu\n", offsetof(struct Example, b)); // 输出 4
    return 0;
}
```

 **__attribute__((packed))** ：取消结构体的**内存对齐优化**

> 仅适用于GCC、Clang等支持GNU扩展的编译器。

```c
struct PackedExample {
    char a;     // 1字节
    int b;      // 4字节（紧接a后，无填充）
    short c;    // 2字节
} __attribute__((packed));

int main() {
    printf("Size: %zu\n", sizeof(struct PackedExample)); // 输出 7（1+4+2）
    return 0;
}
```

使用场景：解析网络协议头

```c
#include <stddef.h>

// 协议头定义（紧密排列）
struct __attribute__((packed)) PacketHeader {
    uint8_t type;      // 1字节
    uint16_t length;   // 2字节
    uint32_t checksum; // 4字节
};

void parse_packet(const uint8_t *data) {
    // 直接按偏移量访问（假设data指向协议头）
    uint16_t len = *(uint16_t *)(data + offsetof(struct PacketHeader, length));
    printf("Length: %u\n", len);
}
```