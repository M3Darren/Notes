##  数组

### 定义&初始化

```c
//1.定义时初始化
double balance[5] = {1000.0, 2.0, 3.4, 7.0, 50.0};
double balance[] = {1000.0, 2.0};
//2.先定义，后初始化;注意balance[]未指明大小的不可先定义后初始化
double n[10];
/* 初始化数组元素 */         
   for ( i = 0; i < 10; i++ )
   {
      n[ i ] = i + 100; /* 设置元素 i 为 i + 100 */
   }
```



### 获取长度

```c
int numbers[] = {1, 2, 3, 4, 5};
int length = sizeof(numbers) / sizeof(numbers[0]);
```

使用宏定义：

```c
#include <stdio.h>

#define LENGTH(array) (sizeof(array) / sizeof(array[0]))
int main() {
    int array[] = {1, 2, 3, 4, 5};
    int length = LENGTH(array);
    prinf("数组长度为: %d\n", length);
    return 0;
}
```



### 详解

- 多维数组

  ```c
  {
      int arr[2][3]={1,2,3,4,5,6}//等同{{1,2,3},{4,5,6}}
  }
  ```

- <span id="ArrToFunc">传递数组给函数 & 函数返回数组</span>

  ```c
  //传递数组
  void func(int arr[2]){};
  void func(int arr[]){};
  void func(int *p){};
  ```

  > [!CAUTION]
  >
  > 不支持在函数外返回局部变量的地址，除非定义局部变量为 **static** 变量

  ```c
  //返回数组
  /* 要生成和返回随机数的函数 */
  int * getRandom( )
  {
    static int  r[10];
    int i;
   
    /* 设置种子：可在全局设置srand，之后每次调用rand()就可以产生随机数 */
    srand( (unsigned)time( NULL ) );
    for ( i = 0; i < 10; ++i)
    {
       r[i] = rand();
       printf( "r[%d] = %d\n", i, r[i]);
    }
   
    return r;
  }
  ```

- 指向数组的指针

  ```c
  {
      double *p;
  	double balance[10];
  	p = balance;//表示balance[0]的地址
      //可通过指针的运算访问其他元素
  	p++;//指针指向下一个元素地址
  }
  ```

- 静态数组 & 动态数组

  ```c
  //静态
  int staticArray[5]; // 静态数组声明
  int staticArray[] = {1, 2, 3, 4, 5}; // 静态数组声明并初始化
  ```

  内存分配：通常在栈上，访问速度快

  大小：固定

  ```c
  //动态
  	int *dynamicArray=(int *)malloc(size * sizeof(int));
  
      if (dynamicArray == NULL) {
          printf("Memory allocation failed.\n");
          return 1;
      }
  	
      printf("Enter %d elements: ", size);
      for (int i = 0; i < size; i++) {
          scanf("%d", &dynamicArray[i]);
      }
  
      printf("Dynamic Array: ");
      for (int i = 0; i < size; i++) {
          printf("%d ", dynamicArray[i]);
      }
      printf("\n");
      free(dynamicArray); // 动态数组内存释放
  ```

  内存分配：通常在堆上

  大小：不固定

