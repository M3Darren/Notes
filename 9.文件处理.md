## 文件处理

> C语言把所有设备都当做文件，通过文件指针进行访问文件

### 标准输入/输出

| 标准文件 | 文件指针 | 设备 |
| :------- | :------- | :--- |
| 标准输入 | stdin    | 键盘 |
| 标准输出 | stdout   | 屏幕 |
| 标准错误 | stderr   | 屏幕 |

- printf( format , ... )

  `...` ：提供要输出的数据。

- scanf( format , ... )

  `...` ：提供存储输入数据的变量**地址**



### 字符（串）输入/输出

单个字符的输入：`int getchar(void)`

单个字符的输出：`int putchar(void)`

**字符串的输入输出**

1. ~~gets()~~和puts()：将字符串输入/输出到标准输入/输出；`gets()`容易导致缓冲区溢出，不建议使用。`puts()`会自动在末尾添加换行符

2. <span id="f">`fgets()和fputs()`</span>：接受任意流输入/输入（可以是标准输入/出流或文件流等）

   ```c
   char *fgets(char *str, int n, FILE *stream);//从流读取数据到str
   int fputs(const char *str, FILE *stream);//将str写入到流
   ```

   > [!CAUTION]
   >
   > 注:`fgets()`读取 n - 1 个字符。它会把读取的字符串复制到缓冲区 **str**，并在最后追加一个 **null** 字符来终止字符串，会在读取到换行符时停止读取；
   >
   > `fputs()` 不会在输出字符串后自动添加换行符

   

   

### 文件输入/输出

- `fopen()和fclose()`：文件打开/关闭

  ```c
  FILE *fopen(const char *filename, const char *mode);
  int fclose(FILE *stream);
  ```

- 读取和写入

  单个字符操作

  ```c
  int fgetc(FILE *fp);//返回值是读取的字符，如果发生错误则返回 EOF
  int fputc(int c , FILE *fp);//如果写入成功，它会返回写入的字符，如果发生错误，则会返回 EOF
  ```

  [参照字符串中fgets和fputs](#f)

  其他操作函数:

  ```c
  int fprintf(FILE *fp,const char *format, ...);//将字符串写入fp
  int fscanf(FILE *fp, const char *format, ...);//从fp读取字符串，遇到第一个空格和换行符时，它会停止读取
  ```



### 二进制I/O

用于存储块的读写 - 通常是数组或结构体

```c
size_t fread(void *ptr, size_t size_of_elements, 
             size_t number_of_elements, FILE *a_file);
              
size_t fwrite(const void *ptr, size_t size_of_elements, 
             size_t number_of_elements, FILE *a_file);
```

