## 共用体

> 区别于结构体，共用体成员使用同一个内存地址；共用体内存大小 = 最大类型成员大小

### 定义

同结构体的定义

```c
union Data
{
   int i;
   float f;
   char  str[20];//该成员代表共用体内存大小
} data;
```



### 访问成员

```c
data.i = 10;
data.f = 220.5;
strcpy( data.str, "C Programming");
//非法，这样使用后达不到预期，只会输出最后一次赋予成员的值
printf( "data.i : %d\n", data.i);//格式化错误
printf( "data.f : %f\n", data.f);//格式化错误
printf( "data.str : %s\n", data.str);//C Programming
```

> [!NOTE]
>
> 在使用共用体时，应该赋值后对其进行操作，后续改变对应后续操作；不可一次性对多个成员赋值，因为内存中只有最后一次赋值的数据

符合预期示例：

```c
	data.i = 10;
   printf( "data.i : %d\n", data.i);//10
   
   data.f = 220.5;
   printf( "data.f : %f\n", data.f);//220.5
   
   strcpy( data.str, "C Programming");
   printf( "data.str : %s\n", data.str);//C Programming
```



