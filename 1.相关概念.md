##  相关概念

> 数据类型，变量，常量，存储类
>

### 数据类型

1. 基本类型：char，int，double，float
2. 枚举类型
3. void类型
4. 派生类型：数组，指针，结构体



#### 基本类型

整数型：

| 类型           | 存储大小    | 值范围                                               |
| :------------- | :---------- | :--------------------------------------------------- |
| char           | 1 字节      | -128 到 127 或 0 到 255                              |
| unsigned char  | 1 字节      | 0 到 255                                             |
| signed char    | 1 字节      | -128 到 127                                          |
| int            | 2 或 4 字节 | -32,768 到 32,767 或 -2,147,483,648 到 2,147,483,647 |
| unsigned int   | 2 或 4 字节 | 0 到 65,535 或 0 到 4,294,967,295                    |
| short          | 2 字节      | -32,768 到 32,767                                    |
| unsigned short | 2 字节      | 0 到 65,535                                          |
| long           | 4 字节      | -2,147,483,648 到 2,147,483,647                      |
| unsigned long  | 4 字节      | 0 到 4,294,967,295                                   |

浮点型：

| 类型        | 存储大小 | 值范围                 | 精度        |
| :---------- | :------- | :--------------------- | :---------- |
| float       | 4 字节   | 1.2E-38 到 3.4E+38     | 6 位有效位  |
| double      | 8 字节   | 2.3E-308 到 1.7E+308   | 15 位有效位 |
| long double | 16 字节  | 3.4E-4932 到 1.1E+4932 | 19 位有效位 |



类型转换：

```sh
#隐式转换（！！！默认转向字节大的类型！！！）
int i = 10;
float f = 3.14;
double d = i + f; // 隐式将int类型转换为double类型

#显示转换
double d = 3.14159;
int i = (int)d; // 显式将double类型转换为int类型
```



#### void类型

- 函数返回为void
- 函数参数为void
- 指针指向为void



#### 枚举类型

> **注意：**第一个枚举成员的默认值为整型的 0，后续枚举成员的值在前一个成员上加 1；
>
> *没有指定值的枚举元素，其值为前一元素加 1*；
>
> 枚举不止可以赋值整型，其他也可

```sh
 /* 1.先定义枚举类型，再定义变量 */
    enum COLOR{
		R,G,B
	};
	enum COLOR color;
	color=R;
    printf("Define first;  %d\n",color);
    
    /* 2.定义枚举类型的同时定义变量 */
    enum COLOR1{
		E=1,Q,D
	} color1;
	color1=Q;
    printf("Definition at the same time;  %d\n",color1);
    
    /* 3.直接定义枚举变量 */
	enum {
		T,P,L
	} color2;
	color2=L;
	printf("Direct define;  %d",color2);
```



###  常量&变量

#### 常量的定义

- 使用`#define`：编译时只是简单的字符替换

  ```sh
  #define PI 3.1415
  ```

- 使用`const`：声明只读变量，编译时会类型检查；（定义时必须初始化）

  ```sh
  const int a=10
  ```



#### 常量类型

**整型常量**：可以是十进制、八进制、十六进制；可以带后缀，U 表示无符号整数（unsigned），L 表示长整数（long）

```sh
212         /* 合法的 */
215u        /* 合法的 */
0xFeeL      /* 合法的 */
30ul        /* 无符号长整数 */
078         /* 非法的：8 不是八进制的数字 */
032UU       /* 非法的：不能重复后缀 */
```

**浮点型常量**：由整数部分、小数点、小数部分和指数部分组成；

- 小数形式

  必须包含小数点，格式为：`整数部分 . 小数部分`；允许省略整数或小数部分（如 `.123` 或 `123.`），但小数点不可缺失。

  ```sh
  # 合法
  12.1
  12.
  .1
  # 非法
  12
  1
  ```

- 指数形式

  使用 `e` 或 `E` 分隔尾数与指数，格式为：`尾数 E 指数`；指数部分需要为整数

  ```sh
  #合法
  12.1e2 //等价1210
  .1e2
  # 非法
  .e1 //缺少尾数
  12.1e1.2 //指数非整数
  ```

**字符常量**：括在单引号中，可用在char类型简单变量中，可以是简单的字符或转义字符

**字符串常量**：扩在双引号中，如 “asd”



#### 变量的定义

```sh
# 先定义，后初始化
int a;
a=1;
#定义时直接初始化
int b=2;
byte c='x';//byte可表示1字节的数据，即可表示无符号的char类型
```

上述变量是定义也是声明，注：`extern int x`表示引用其他文件的变量，只是声明，定义在其他文件；



#### 变量类型

**全局变量**：定义在函数之外

**局部变量**：定义在函数之内

注：当全局变量与局部同名时，优先使用局部变量



### 存储类

- auto：是所有`局部变量默认`的存储类

  ```c
  {
     int mount;
     auto int month;
  }
  ```

- register：用于定义存储在寄存器中而不是 RAM 中的局部变量

  ```c
  {
     register int  miles;
  }
  ```

- static：指示编译器在程序的生命周期内保持局部变量的存在，而不需要在每次它进入和离开作用域时进行创建和销毁。因此，使用 static 修饰局部变量可以在函数调用之间保持局部变量的值。

  ```c
  void func1(void)
  {
  /* 'thingy' 是 'func1' 的局部变量 - 只初始化一次
   * 每次调用函数 'func1' 'thingy' 值不会被重置。
   * 每次调用函数 'func1' 'count' 值会被重置为0。
   */                
    static int thingy=5;
    int count=0;
    count--;
    thingy++;
    printf(" thingy 为 %d，count为 %d \n", thingy,count);
  }
  int main()
  {
    while (count--) {
        func1();
    }
    return 0;
  }
  ```

- extern：用于定义在其他文件中声明的全局变量或函数；不会为变量分配任何存储空间，指示编译器该变量在其他文件中定义。

  ```c
  extern void write_data();
  extern char Handle;
  ```

