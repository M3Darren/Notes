## 位域

> 特殊的结构体成员，允许我们按位对成员进行定义，指定其占用的位数。

### 定义

> [!NOTE]
>
> 类型只能为： int(整型)，unsigned int(无符号整型)，signed int(有符号整型) 

```c
struct bit_layout {
    unsigned int a : 4;
    unsigned int b : 4;
    unsigned int c : 3;
} bl,*bp;
```

### 访问成员

```c
//注意赋值时不能超过位域范围
bl.a=4;
bl.b=7;
bl.c=8;
printf( "Sizeof( Age ) : %d\n", sizeof(Age) );
printf( "%d\n", bl.a);
printf( "%d\n", bl.b);
printf( "%d\n", bl.c);// 8的二进制表示为 1000 有四位，超出

//指向位域变量的指针，访问使用`->`
bp=&bl;
printf( "%d\n", bp->a);
```

### 位域特性

- 未命名位域：指定下一位域的偏移量

  ```c
  struct example {
      unsigned int a : 4;  // 4位
      unsigned int   : 2;  // 2位未使用
      unsigned int b : 2;  // 2位
  };
  ```

- 0宽度位域：会强制下一位域从新的存储单元开始（对齐）

  ```c
  struct example {
      unsigned int a : 4;
      unsigned int   : 0;  // 强制对齐到下一个存储单元(字节)
      unsigned int b : 4;//其实偏移为9
  };
  ```

  

### 注意事项

1. **取地址操作**：不能对`位域成员`使用取地址运算符(&)

2. **数组和指针**：不能创建位域数组或指向位域的指针

3. **类型限制**：位域通常只能用于整数类型

4. **可移植性**：位域的具体实现依赖于编译器和平台

5. **符号处理**：`signed int`位域的最高位是符号位

6. **内存分布**：取决于系统架构或编译器；若想保证可移植性，通过`#define`实现

   ```c
   #define ERROR_FLAG   (1 << 0)
   #define READY_FLAG   (1 << 1)
   #define BUSY_FLAG    (1 << 2)
   
   unsigned int flags;
   
   // 设置标志
   flags |= ERROR_FLAG;
   
   // 清除标志
   flags &= ~READY_FLAG;
   
   // 检查标志
   if (flags & BUSY_FLAG) {
       // 忙状态处理
   }
   ```

   