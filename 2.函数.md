## 函数

### 声明&定义

> [!NOTE]
>
> 被调用函数在当前函数之前已经定义则不需要声明

```c
#include <stdio.h>
/* 函数声明 */
int max(int num1, int num2);
 
int main ()
{
   /* 局部变量定义 */
   int a = 100;
   int b = 200;
   int ret;
   /* 调用函数来获取最大值 */
   ret = max(a, b);
   printf( "Max value is : %d\n", ret );
   return 0;
}
 
/* 函数的定义：返回两个数中较大的那个数 */
int max(int num1, int num2) 
{
   /* 局部变量声明 */
   int result;
   if (num1 > num2)
      result = num1;
   else
      result = num2;
   return result; 
}
```

### 参数

函数参数传递的方式：

- 传值调用

  > [!IMPORTANT]
  >
  > 把参数的实际值复制给函数的形式参数，修改函数内的形式参数不会影响实际参数。

  ```c
  void m_swap(int a,int b){
      int tmp;
      tmp=a;
      a=b;
      b=tmp;
  }
  int main(){
      int a=1,b=2;
      m_swap(a,b);
      printf("a:%d,b:%d",a,b);//此时实参值不会改变
  }
  ```

  

- <span id="section2">引用调用</span>

  > [!IMPORTANT]
  >
  > 通过指针传递方式，形参为指向实参地址的指针，当对形参的指向操作时，等于对实参本身操作。

  ```c
  void m_swap(int *a,int *b){
      int tmp;
      tmp=*a;
      *a=*b;
      *b=tmp;
  }
  int main(){
      int a=1,b=2;
      m_swap(&a,&b);
      printf("a:%d,b:%d",a,b);//此时实参值改变，传递的是地址，改变了地址中的值
  }
  ```

  